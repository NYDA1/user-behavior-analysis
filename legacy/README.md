# Legacy: Thesis Implementation (Original Scripts)

The original scripts written for the graduation thesis, archived verbatim —
not part of the current pipeline. They analyze the same dataset with the
same methodology (30-minute sessionization, funnel analysis, three loss-path
patterns) but on an earlier sequential sample of 983 users.

- `explore.py` ... `sample_validation.py` — the 11 original analysis scripts
- `archive/` — original chart and intermediate CSV outputs produced by them

Refer to the root `README.md` for the current, reproducible pipeline.
